import { SaveMenuOpenStateDirective } from './save-menu-open-state.directive';

describe('SaveMenuOpenStateDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveMenuOpenStateDirective();
    expect(directive).toBeTruthy();
  });
});
