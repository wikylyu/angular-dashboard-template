import { AdminUserStatusNamePipe } from './admin-user-status-name.pipe';

describe('AdminUserStatusNamePipe', () => {
  it('create an instance', () => {
    const pipe = new AdminUserStatusNamePipe();
    expect(pipe).toBeTruthy();
  });
});
